import { render } from '@testing-library/react'

import '@testing-library/jest-dom'

import { default as {{ properCase name }} } from './{{ properCase name }}'

describe('{{ properCase name }}', () => {
    it('renders the todo app', () => {
      render(<{{ properCase name }} />);
    })
})